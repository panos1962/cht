#!/usr/bin/env bash

[ -z "CHT_BASEDIR" ] &&
CHT_BASEDIR="/var/opt/cht"

vigladir="${CHT_BASEDIR}/misc/vigla"

pd_sqlplus -F "${CHT_BASEDIR}/private/opsoidb.cf" \
"${vigladir}/lib/vigla.sql" |\
sort -t "	" |\
awk -f "${vigladir}/lib/vigla.awk" "${vigladir}/lib/spoudes.tsv" -
